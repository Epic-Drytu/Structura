# Core data stack
pandas>=2.2.0
numpy>=1.26.0

# Database
# sqlite3 is built into Python — no install needed

# Visualisation (for report plots)
matplotlib>=3.8.0
seaborn>=0.13.0

# HTML report generation
jinja2>=3.1.0

# CLI
click>=8.1.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Optional: smarter imputation (MAR strategy)
scikit-learn>=1.4.0

# Utilities
tqdm>=4.66.0          # progress bars on large files
python-dotenv>=1.0.0  # for any env config later